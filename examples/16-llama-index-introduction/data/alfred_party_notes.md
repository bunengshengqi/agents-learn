# Alfred 派对助手资料

Alfred 正在为 Wayne Manor 策划一场超级英雄主题派对。

派对报名表要求来宾填写姓名、邮箱、角色装扮和到达时间。

如果网页表单显示 `Please enter a valid email address`，最常见的原因是邮箱格式不合法，例如缺少 `@` 或域名。

视觉智能体处理图片时，如果底层模型不能直接看图，可以先让视觉工具把图片转成文字 observation，再让文本模型推理。

多智能体系统里，Manager Agent 负责拆解任务、委派子 Agent、收集结果并输出最终答案。
